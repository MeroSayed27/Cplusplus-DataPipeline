#include <cassert>
#include <iostream>
#include "pipeline.h"

int main() {
    // Basic test placeholder
    runPipeline();

    // Example assertion (replace with real checks later)
    assert(true);

    std::cout << "✅ Test passed: pipeline executed without errors." << std::endl;
    return 0;
}
